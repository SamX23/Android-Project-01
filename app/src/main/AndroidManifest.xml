<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.samdev.project01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".option_menu.AboutUs" />
        <activity android:name=".content.Activity01" />
        <activity android:name=".content.Activity02" />
        <activity android:name=".content.Activity03" />
        <activity android:name=".content.Activity04" />
        <activity android:name=".content.Activity05" />
    </application>

    <dist:module dist:instant="true" />

</manifest>